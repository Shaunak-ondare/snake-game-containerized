version: '3.8'

services:
  backend:
    build:
      context: ./snake-game/backend
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build:
      context: ./snake-game/client
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
